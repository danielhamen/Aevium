# Aevium `/app` directory

The `/app` directory contains the code for the Aevium.io web app. Inside the `/app/dev` directory, you will find development code; inside the `/app/www`, you will find production code.
